var b = 6;

fun f(x, y) = 
    var a = x + y * 2 / 3 - 1;
    print(a);
    if
        x > 4 && !(~x <= 4 || b < 3)
    then
        return a * a;
    elif
        1
    then
        return y & 2 << 1 + x;
    else
        return y - 1;
    end
end

fun main() =
    var x = 5 + 2 / -2 * +2;
    while
        x > 0
    do
        x = f(x, (x + 1));
    end
    x = input();
    return 0;
end
